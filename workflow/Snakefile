configfile: "workflow/config/config.yaml"


include: "rules/common.smk"
include: "rules/setup.smk"
include: "rules/align_hic.smk"

rule all:
    params:

    input:
        [result[0] for result in EXPERIMENTS.FormatTemplate("results/{experiment}/{replicate}/matrix/{replicate}_ds{downsample}.mcool", downsample = config['downsample'])]
